type Product = {
    id: string
    title: string
    description: string
    slug: string
    category: string[]
}

type Category = {
    id: string
    title: string
    slug: string
}

type Account = {
    id: string
    uid: string
    name: string
}